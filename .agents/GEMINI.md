# Quizdy — Gemini Instructions

Read and follow the project guidelines defined in `.agents/AGENTS.md` before making any changes.

For repetitive tasks, check `.agents/workflows/` for existing workflows before proceeding manually.

**CRITICAL INSTRUCTION FOR NEW FILES**: Every time you create a NEW `.dart` source file, you MUST include the GPL-3.0 license header at the very top of the file, exactly as defined in `AGENTS.md`. DO NOT skip adding the license header to new files.
