# Normalize text files to LF in the repository and avoid platform-specific EOL diffs
* text=auto eol=lf

# Keep Windows script files with CRLF (avoids changes when editing on Windows)
*.ps1 text eol=crlf
*.bat text eol=crlf

# Mark common binary files as binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.jar binary
*.aar binary
*.so binary
*.dll binary

# Dart/Flutter generated artifacts (optional) - treat as binary
.flutter-plugins-dependencies binary
.packages binary
.pub-cache/ binary

# End of file
